import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{

        backgroundColor: "#0948A2",
        height: 50,
        borderRadius: 8,
        alignItems: "center",
        justifyContent: "center",
    },
    title: {
        color: "#fff",
       //fontSize: 19.92,//O fontSize agora é passado por props
        fontWeight: 400,
    },

});